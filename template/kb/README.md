# KB — Knowledge Base Index

This directory contains SOPs (Standard Operating Procedures) and runbooks
generated during development sessions. Check here before starting any
non-trivial task.

| File | What it covers | Last updated |
|------|---------------|-------------|
| *(empty — add SOPs as they are created)* | | |

---

## Adding a new entry

When an agent completes a non-trivial action and the human approves saving it:

1. Create `kb/sop-<action-name>.md` using the template in the agent file
2. Add a row to this table

## Runbook format

```markdown
# SOP: <title>

## When to use
## Prerequisites
## Inputs required
## Steps
## Expected output
## Known failure modes
```
