# .overview.md — <project-name>

**L1 context file.** This is the entry point for any agent orienting in the
project. For an ultra-concise project map, read `.abstract.md` (L0) first.
Subsystems each have their own `.overview.md` for deeper detail; read the
relevant one before working inside that subsystem.

---

## What this project is

<!-- One paragraph: what this project does, why it exists, who uses it -->

---

## Repository map

| Subsystem | Path | Type | Purpose |
|-----------|------|------|---------|
| <!-- fill in --> | `<subsystem>/` | module | ... |

---

## Subagent quick-reference

| Task type | First agent to invoke |
|-----------|----------------------|
| Runtime error / crash / metrics | `@<project>-sre` |
| Infra / env / secrets / CI | `@<project>-devops` |
| <subsystem> code or behaviour | `@<project>-<role>` |

---

## Service endpoints

| Service | URL | Notes |
|---------|-----|-------|
| <!-- fill in --> | http://... | |

---

## Stack at a glance

| Layer | Technology |
|-------|-----------|
| <!-- fill in --> | ... |

---

## Dev workflow

```sh
# Run tests
# <command>

# Start local stack
# <command>

# Apply a change
# <command>
```

---

## Storage / persistence

<!-- Describe any persistent state: databases, volumes, files, caches -->

---

## Non-negotiables

<!-- Project-wide rules that must always hold -->
- Never commit unencrypted secrets
- Never use floating/latest versions

---

## Subsystem context files

Each subsystem has its own `.overview.md` (L1) and `.abstract.md` (L0). Read the
relevant one before working inside that subsystem:

| Subsystem | L1 file | L0 file |
|-----------|---------|---------|
| <!-- fill in --> | `<subsystem>/.overview.md` | `<subsystem>/.abstract.md` |
