# example-resource.md

This is a supporting file for the `example-skill` skill. Replace this with
a real resource your skill needs at runtime — for example:

- A reference table the skill looks up during execution
- A template the skill renders and fills in
- A checklist the skill works through step by step
- A script the skill calls via Bash

## Example: lookup table

| Key | Value | Notes |
|-----|-------|-------|
| `example-key` | `example-value` | replace with real entries |

## Example: checklist

- [ ] Step one
- [ ] Step two
- [ ] Step three

Reference this file from `SKILL.md` with `./example-resource.md`.
