{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "orchestrator",
  "mcp": {
    "REPLACE_ME": {
      "type": "local",
      "command": ["npx", "-y", "REPLACE_WITH_MCP_SERVER"],
      "enabled": true
    }
  }
}
